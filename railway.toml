[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Redeploy trigger: /reiniciar + tag fixes + pub dates — 2026-08-14
