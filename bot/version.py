"""Deployed bot version string (shown in /ping and logs)."""

BOT_VERSION = "2026-08-16-classify-all-country-pending"

# Fallback when GitHub API is unavailable (update on each deploy PR).
LAST_PR_NUMBER = 48
LAST_PR_TITLE = "Clasificar pendientes en informes por país/tag (evitar vacíos)"
