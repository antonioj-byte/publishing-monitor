[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Redeploy trigger: fix /reclasificar import + resolve merge markers — 2026-08-15
